# SDK 开发位置

参赛入口已实现为此目录的 `main3.py`，本项目启动脚本是 `../run.sh`。目前未收到官方 SDK 压缩包或原始 run.sh。
对话提供的入口源代码完整保存在 `docs/source/main3_original.py`（仓库根目录相对路径）。

完整实现方案见 [SDK 设计](../../docs/SDK_DESIGN.md)，开发与验收顺序见 [实施计划](../../docs/SDK_VALIDATION_PLAN.md)。当前实现与真实验证记录见 [实现状态](../../docs/IMPLEMENTATION_STATUS.md)。

已保留 `POST /` 和 `callback(json_data)`；响应只输出 `roleCommandMap`、`prompt`、`executeCmd`。原始入口在 docs/source 中留档，运行入口不再回显 Request。

## 比赛诊断日志

按原方式 `bash SDK_Python/run.sh <port>` 启动即可。SDK 默认使用
`print(..., flush=True)` 输出日志，由比赛平台收集，不写日志文件、不额外打包。
赛后直接复制平台上的精简 `HUNTER` 日志即可。

默认是适合复制粘贴的 `compact` 模式：每行以 `HUNTER ` 开头，后面是独立JSON，
`schema=2`。启动一行、首个回合一行，之后每20个有效回合一条摘要；
每20回合最多另输出两条关键变化，首个程序异常另有一条保留名额。
重复失败合并计数，成功POST不再逐次打印访问记录。

摘要保留金币、积分、基地血量、角色位置、动作短描述、任务进展和失败原因；
失败动作附前次目标与起点，攻击失败附控制距离/射程/冷却检查，判题错误与动作受理分开。
完整地图、商店、模型输入输出、沙盒正文与长堆栈默认收起；日志过长时优先保留问题和计数。
复制启动行及问题附近的 `HUNTER` 行和下一条摘要即可，不需要上传文件。

日志不改变 HTTP 响应的三个字段。打印失败不会使本来合法的动作失败。
若下一场需要完整采集，启动前设置 `HUNTER_LOG_MODE=full`，恢复原有
`schema=1`、`run/seq`、`part/parts`、`payload` 分段格式。省略的正文无法从精简摘要恢复。
详见工作区 [日志说明](../../docs/SDK_LOGGING.md)。


## 2026-09-11 实战修订

任务沙盒从 `/` 启动时，引用的 `.md` 任务文件会先在当前非根目录和实测
`/tmp/selfEvolutionTask` 下有限定位，再读取内容。找不到、歧义或空题目不会提交错误信息。
采矿、出售、建造和返防共享失败移动的绕行记录；可选围墙为经济活动留下工人。
已有两座武器且存在药店和 Medicine 报价时，建造预算保留默认20金币，收入到账后再补建。
开拓者卡路且受威胁时提前考虑撤离，没有分配武器时仍规划返程。

R23—R29补充修订：长期移动退避统一应用到候选与降级方案，
`movement_retry_windows`记录允许重试的回合；答案提交的动作受理和判题错误分开保存，
具体错误摘要传给下一次任务推理，跳帧反馈不强行归因。
任务清空且收到timeout时明确记录超时，迟到模型回复不进入下一题。

启动日志应出现 `HUNTER` 的 `startup` 事件：精简模式用 `sdk` 表示源码清单摘要，
完整模式保留实际入口路径和 `file_manifest_sha256`；比赛上传包要与源码指纹一致。
比赛片段中原程序打印的是 `[REQ]`、蓝16/黄24，与本实现有差异，不能混用版本归因。
完整分析与设计修改见工作区 `docs/MATCH_2026-09-11_REVIEW.md`。

## b5ab1470 实战反馈修订

优先补齐三炮，一名工人负责采石筑墙，另一名采卖；共同分配可达矿点。健康炮手优先坚守，持续掉血与低血量时治疗/撤离，连续观测下避免再次进入危险格；基地周围敌人提高优先级。任务新增经比赛协议跨回合执行的`run_python`操作，完成实际API查询、统计和工作区修复；任务概述不作为答案提交。没有修改官方建筑成本或机器人目标规则。

日志仍默认每20回合精简摘要，补充`walls`数量与`guns`类型/坐标。启动方式、`callback(json_data)`、Flask `POST /`、三个响应字段保持兼容。详细验证记录位于本地工作区`docs/MATCH_b5ab1470_REVIEW.md`。
